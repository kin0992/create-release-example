{
  "name": "create-release-example",
  "version": "4.4.0",
  "description": "A simple project used to make an example on how to automate the release.",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc",
    "version": "changeset version && npm install",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "typescript": "^5.1.3"
  }
}
